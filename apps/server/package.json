{
  "name": "proline-server",
  "version": "0.0.1",
  "description": "Minimal Proline NestJS API",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "nest build",
    "dev": "nest start --watch src/main-dev.ts",
    "start": "node dist/main-dev.js",
    "compose:up": "docker compose -f docker-compose.yml up -d",
    "compose:down": "docker compose -f docker-compose.yml down",
    "prisma:generate": "prisma generate",
    "prisma:format": "prisma format"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.14",
    "@nestjs/core": "^11.0.14",
    "@nestjs/platform-express": "^11.1.6",
    "@prisma/client": "^6.6.0",
    "dotenv": "^16.6.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.6",
    "@types/node": "^22.18.0",
    "prisma": "^6.6.0",
    "typescript": "^5.8.3"
  }
}
